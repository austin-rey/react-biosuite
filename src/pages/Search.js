import React from 'react'
import PropTypes from 'prop-types'

const Search = ({type}) => {
    return (
        <div>
            <h1>Search Page</h1>
        </div>
    )
}

Search.propTypes = {
    type: PropTypes.string
}

export default Search
